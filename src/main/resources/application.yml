spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  datasource:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true

mqtt:
  broker:
    host: damoa.io
    port: 1883
  qos: 2
  topic: "ewha/power/washer/data/1"

